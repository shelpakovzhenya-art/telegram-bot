# ✅ Бот настроен и готов к запуску!

## 🎯 Токен бота уже добавлен в .env

Токен вашего бота уже настроен в файле `.env`.

## 🚀 Быстрый запуск

### Вариант 1: Через PowerShell (рекомендуется)
```powershell
.\START_BOT.ps1
```

### Вариант 2: Через командную строку
```cmd
START_BOT.bat
```

### Вариант 3: Вручную
```powershell
python -m app.main
```

## 📋 Перед первым запуском

1. **Установите зависимости** (если еще не установлены):
   ```powershell
   pip install -r requirements.txt
   ```

2. **Убедитесь, что Python 3.12+ установлен:**
   ```powershell
   python --version
   ```

## 📱 Добавление бота в Telegram

### Шаг 1: Создайте канал с обсуждениями

1. Создайте новый канал в Telegram
2. Перейдите в настройки канала
3. Включите "Обсуждения" (Discussions)
4. Выберите или создайте группу для обсуждений

### Шаг 2: Добавьте бота в группу

1. Откройте группу обсуждений
2. Нажмите на название группы → "Добавить участников"
3. Найдите вашего бота по username (который вы указали при создании у BotFather)
4. Добавьте бота

### Шаг 3: Выдайте права администратора

1. Нажмите на бота в списке участников
2. Выберите "Права администратора"
3. **Обязательно включите:**
   - ✅ **"Ограничивать участников"** (Restrict members)
4. **Желательно включить:**
   - ✅ "Удалять сообщения" (Delete messages)
5. Нажмите "Сохранить"

### Шаг 4: Проверьте работу

1. Запустите бота (см. "Быстрый запуск" выше)
2. В группе:
   - Добавьте нового участника — бот должен поприветствовать
   - Напишите `/help` — бот должен ответить
   - Ответьте на сообщение со словом "спасибо" — добавится карма

## 🎮 Команды бота

- `/start` или `/help` — справка
- `/karma` — показать свою карму
- `/karma [reply]` — показать карму пользователя
- `/top` — топ-10 по карме
- `/warn [reply] [причина]` — выдать предупреждение (админы)
- `/warns [reply]` — показать предупреждения
- `/unwarn [reply]` — снять предупреждение (админы)

## ⚙️ Настройка (опционально)

Отредактируйте файл `.env` для настройки:
- `ALLOWED_CHAT_IDS` — ограничить работу только в определенных чатах
- `KARMA_COOLDOWN_MINUTES` — кулдаун между начислениями кармы (по умолчанию 60)
- `WARN_LIMIT` — лимит предупреждений перед мутом (по умолчанию 3)
- `MUTE_HOURS` — длительность мута (по умолчанию 24 часа)

## 🐛 Проблемы?

### Бот не запускается
- Проверьте, что Python установлен: `python --version`
- Установите зависимости: `pip install -r requirements.txt`

### Бот не отвечает в группе
- Убедитесь, что бот добавлен в группу
- Проверьте, что бот имеет права администратора
- Убедитесь, что бот запущен (окно с логами должно быть открыто)

### Мут не работает
- Проверьте, что бот имеет право "Ограничивать участников"
- Посмотрите логи бота на наличие ошибок

---

**Готово! Бот настроен и готов к работе! 🎉**

