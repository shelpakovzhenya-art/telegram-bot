# 🐍 Развертывание бота на PythonAnywhere (БЕСПЛАТНО)

PythonAnywhere предлагает бесплатный план для постоянной работы бота.

## 📋 Подготовка

1. ✅ Аккаунт на [PythonAnywhere](https://www.pythonanywhere.com) (бесплатная регистрация)
2. ✅ Токен бота от [@BotFather](https://t.me/BotFather)
3. ✅ Код проекта в GitHub репозитории

## 🚀 Пошаговая инструкция

### Шаг 1: Регистрация

1. Перейдите на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Нажмите **"Sign up for free"**
3. Заполните форму регистрации
4. Подтвердите email

### Шаг 2: Создание Web App (для постоянной работы)

1. В Dashboard нажмите **"Web"** в верхнем меню
2. Нажмите **"Add a new web app"**
3. Выберите **"Manual configuration"**
4. Выберите **"Python 3.10"** (или последнюю доступную версию)
5. Нажмите **"Next"**

### Шаг 3: Настройка Web App

1. В разделе **"Source code"** укажите путь: `/home/ваш_username/telegram-bot`
2. В разделе **"Working directory"** укажите тот же путь

### Шаг 4: Клонирование репозитория

1. Откройте вкладку **"Files"**
2. Откройте **"Bash"** (консоль)
3. Выполните команды:

```bash
cd ~
git clone https://github.com/shelpakovzhenya-art/telegram-bot.git
cd telegram-bot
pip3.10 install --user -r requirements.txt
```

### Шаг 5: Настройка переменных окружения

1. В консоли создайте файл `.env`:
```bash
nano .env
```

2. Добавьте содержимое:
```
BOT_TOKEN=8449446845:AAHQLxHSFvR6NeGSOzEto2HoczJbeJFhv0E
```

3. Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6: Создание WSGI файла

1. В разделе **"Web"** найдите **"WSGI configuration file"**
2. Нажмите на ссылку
3. Замените содержимое на:

```python
import sys
import os

# Add your project directory to the path
path = '/home/ваш_username/telegram-bot'
if path not in sys.path:
    sys.path.insert(0, path)

# Set environment variables
os.environ['BOT_TOKEN'] = '8449446845:AAHQLxHSFvR6NeGSOzEto2HoczJbeJFhv0E'

# Import and run your application
from app.main import main
import asyncio

# Run the bot
asyncio.run(main())
```

**ВАЖНО:** Замените `ваш_username` на ваш username на PythonAnywhere!

### Шаг 7: Настройка Always-On Task (лучший вариант)

Вместо Web App лучше использовать Always-On Task:

1. В Dashboard нажмите **"Tasks"**
2. Нажмите **"Create a new task"**
3. Выберите **"Always-on task"**
4. В поле **"Command"** введите:
```bash
cd ~/telegram-bot && python3.10 -m app.main
```
5. Нажмите **"Create"**

### Шаг 8: Настройка переменных окружения для Task

1. В консоли (Bash) выполните:
```bash
cd ~/telegram-bot
nano .env
```

2. Добавьте:
```
BOT_TOKEN=8449446845:AAHQLxHSFvR6NeGSOzEto2HoczJbeJFhv0E
```

3. Сохраните

### Шаг 9: Запуск

1. В разделе **"Tasks"** найдите ваш Always-On Task
2. Нажмите **"Run"** или он запустится автоматически
3. Проверьте логи в разделе **"Tasks"**

## 💰 Бесплатный план PythonAnywhere

**Что включено:**
- ✅ 1 Always-On Task (работает постоянно)
- ✅ 512MB дискового пространства
- ✅ Достаточно для Telegram-бота

**Ограничения:**
- 1 Always-On Task одновременно
- Ограниченное дисковое пространство

## ✅ Готово!

Бот работает на PythonAnywhere 24/7! 🎉

---

## 🔧 Альтернатива: Простой VPS

Если PythonAnywhere не подходит, можно использовать:
- **Oracle Cloud Free Tier** - бесплатный VPS навсегда
- **AWS Free Tier** - 12 месяцев бесплатно
- **Google Cloud Free Tier** - $300 кредитов


