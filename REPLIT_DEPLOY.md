# 🚀 Развертывание бота на Replit (БЕСПЛАТНО, БЕЗ КАРТЫ)

Replit предлагает бесплатный план без требования кредитной карты - идеально для России!

## 📋 Подготовка

1. ✅ Аккаунт на [Replit](https://replit.com) (можно зарегистрироваться через GitHub)
2. ✅ Токен бота от [@BotFather](https://t.me/BotFather)
3. ✅ Код проекта в GitHub репозитории

## 🚀 Пошаговая инструкция

### Шаг 1: Регистрация на Replit

1. Перейдите на [replit.com](https://replit.com)
2. Нажмите **"Sign up"**
3. Выберите **"Sign up with GitHub"** (рекомендуется)
4. Разрешите доступ к репозиториям

### Шаг 2: Создание Repl из GitHub

1. В Dashboard нажмите **"Create Repl"**
2. Выберите **"Import from GitHub"**
3. Введите URL репозитория: `https://github.com/shelpakovzhenya-art/telegram-bot`
4. Нажмите **"Import"**

### Шаг 3: Настройка Repl

1. Replit автоматически определит Python проект
2. Если нет, выберите **"Python"** в настройках

### Шаг 4: Добавление переменных окружения

1. В левой панели найдите иконку **"Secrets"** (замочек) или перейдите в **"Secrets"** в меню
2. Нажмите **"New secret"**
3. Добавьте:
   - **Key:** `BOT_TOKEN`
   - **Value:** `8449446845:AAHQLxHSFvR6NeGSOzEto2HoczJbeJFhv0E`
4. Нажмите **"Add secret"**

### Шаг 5: Создание файла .replit (если нужно)

Создайте файл `.replit` в корне проекта:

```toml
run = "python -m app.main"
```

### Шаг 6: Установка зависимостей

1. В консоли Replit выполните:
```bash
pip install -r requirements.txt
```

2. Создайте директорию для данных:
```bash
mkdir -p data
```

### Шаг 7: Запуск бота

1. Нажмите кнопку **"Run"** (зеленая кнопка вверху)
2. Бот запустится и будет работать

### Шаг 8: Настройка Always-On (важно!)

**ВАЖНО:** На бесплатном плане Replit останавливает Repl после 1 часа бездействия.

**Решение - использовать UptimeRobot или подобный сервис:**

1. Зарегистрируйтесь на [UptimeRobot.com](https://uptimerobot.com) (бесплатно)
2. Создайте HTTP Monitor
3. Укажите URL вашего Repl (Replit дает публичный URL)
4. UptimeRobot будет "будить" ваш Repl каждые 5 минут

**Или используйте Replit Keep-Alive:**

Создайте файл `keep_alive.py`:

```python
from flask import Flask
from threading import Thread
import time

app = Flask('')

@app.route('/')
def home():
    return "Bot is running!"

def run():
    app.run(host='0.0.0.0', port=8080)

def keep_alive():
    t = Thread(target=run)
    t.daemon = True
    t.start()

if __name__ == '__main__':
    keep_alive()
    # Запустите ваш бот здесь
    from app.main import main
    import asyncio
    asyncio.run(main())
```

И измените `app/main.py` чтобы использовать keep_alive.

## 💰 Бесплатный план Replit

**Что включено:**
- ✅ Бесплатный хостинг
- ✅ Не требует кредитную карту
- ✅ Работает из России
- ✅ Автоматическое развертывание из GitHub

**Ограничения:**
- Repl останавливается после 1 часа бездействия
- Нужен Keep-Alive сервис для постоянной работы

## ✅ Готово!

Бот работает на Replit! Используйте UptimeRobot для поддержания работы 24/7.

---

## 🔧 Альтернатива: Запуск на своем компьютере 24/7

Если у вас есть компьютер, который всегда включен, можно запустить бота локально:

1. Установите Python
2. Запустите бота: `python -m app.main`
3. Оставьте компьютер включенным

Бот будет работать пока компьютер включен.


