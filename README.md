# Telegram Group Bot

Production-ready Telegram-бот для групп/чатов (supergroup), привязанных к каналам. Бот работает сразу после добавления в чат без ручной настройки (кроме переменных окружения).

## 🚀 Функции

### 1. Приветствие новых участников
- Автоматическое приветствие при входе нового участника
- Защита от спама: не приветствует ботов и повторные входы в течение 10 минут (настраивается)

### 2. Карма за "спасибо"
Два способа начисления кармы:

**Ответом на сообщение:**
- Ответьте на сообщение со словами "спасибо", "спс", "благодарю", "thx", "thanks"
- Автору сообщения добавится +1 карма

**Через упоминание:**
- Напишите "спасибо @username" или "спасибо имя"
- Упомянутому пользователю добавится +1 карма

**Правила:**
- Нельзя начислять карму самому себе
- Нельзя начислять карму ботам
- Анти-накрутка: один пользователь может дать карму другому не чаще 1 раза в 60 минут (настраивается)

**Команды:**
- `/karma` — показать свою карму
- `/karma [reply]` — показать карму пользователя (ответ на сообщение)
- `/karma @username` — показать карму указанного пользователя
- `/top` — топ-10 по карме в этом чате

Карма считается отдельно для каждого чата.

### 3. Система предупреждений /warn

**Команды (только для админов):**
- `/warn [reply] [причина]` — выдать предупреждение пользователю (ответ на сообщение)
- `/warn @username [причина]` — выдать предупреждение указанному пользователю
- `/warns [reply]` — показать количество предупреждений пользователя
- `/warns @username` — показать предупреждения пользователя
- `/unwarn [reply]` — снять 1 предупреждение
- `/unwarn @username` — снять 1 предупреждение

**Автоматическое наказание:**
- При достижении лимита предупреждений (по умолчанию 3) пользователь получает мут на 24 часа (настраивается)
- Бот автоматически проверяет права на ограничение участников

## 📋 Требования

- Python 3.12+
- Telegram бот токен (получить у [@BotFather](https://t.me/BotFather))
- Права администратора в чате (для функций модерации)

## 🔧 Установка и настройка

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен — он понадобится для настройки

### 2. Добавление бота в чат

**Важно:** Бот работает в **чате при канале** (supergroup), а не в самом канале.

1. Добавьте бота в ваш чат (discussion chat для канала)
2. Выдайте боту права администратора:
   - **Обязательно:** "Restrict members" (ограничивать участников) — для функции мута
   - **Желательно:** "Delete messages" (удалять сообщения) — для будущих расширений
3. Бот начнет работать автоматически

### 3. Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
   Или на Windows:
   ```powershell
   Copy-Item .env.example .env
   ```

2. Отредактируйте `.env` и укажите:
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   ```

3. Опционально настройте другие параметры:
   - `ALLOWED_CHAT_IDS` — список ID чатов через запятую (если пусто — работает во всех чатах)
   - `KARMA_COOLDOWN_MINUTES` — кулдаун между начислениями кармы (по умолчанию 60)
   - `WARN_LIMIT` — лимит предупреждений перед мутом (по умолчанию 3)
   - `MUTE_HOURS` — длительность мута в часах (по умолчанию 24)
   - `GREETING_COOLDOWN_MINUTES` — кулдаун между приветствиями (по умолчанию 10)

### 4. Получение Chat ID

Чтобы ограничить бота конкретными чатами, нужно узнать ID чата:

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в ваш чат
2. Бот покажет ID чата
3. Укажите его в `ALLOWED_CHAT_IDS` в `.env` (через запятую для нескольких чатов)

## 🚀 Запуск

### Локальный запуск

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Создайте директорию для базы данных (уже создана):
   ```bash
   mkdir -p data
   ```

3. Запустите бота:
   ```bash
   python -m app.main
   ```

### Запуск через Docker Compose

1. Убедитесь, что файл `.env` настроен
2. Запустите:
   ```bash
   docker compose up --build
   ```

3. Для запуска в фоне:
   ```bash
   docker compose up -d --build
   ```

4. Просмотр логов:
   ```bash
   docker compose logs -f
   ```

## 📊 База данных

По умолчанию используется SQLite (файл `data/app.db`). База данных создается автоматически при первом запуске.

### Переключение на PostgreSQL

1. Измените `DATABASE_URL` в `.env`:
   ```env
   DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
   ```

2. Установите зависимости для PostgreSQL (уже включены в `requirements.txt`)

3. Запустите миграции (если используете Alembic):
   ```bash
   alembic upgrade head
   ```

## 🧪 Тестирование

Запуск тестов:
```bash
pytest tests/
```

## 📝 Команды бота

- `/start` или `/help` — показать справку
- `/karma` — показать свою карму
- `/karma [reply]` — показать карму пользователя
- `/karma @username` — показать карму пользователя
- `/top` — топ-10 по карме
- `/warn [reply] [причина]` — выдать предупреждение (админ)
- `/warn @username [причина]` — выдать предупреждение (админ)
- `/warns [reply]` — показать предупреждения
- `/warns @username` — показать предупреждения
- `/unwarn [reply]` — снять предупреждение (админ)
- `/unwarn @username` — снять предупреждение (админ)

## 🏗️ Архитектура

```
app/
  main.py              # Точка входа
  core/
    settings.py        # Настройки (pydantic-settings)
    logging.py         # Структурированное логирование
  bot/
    dispatcher.py      # Настройка диспетчера
    middlewares.py     # Middleware для БД и фильтрации
  handlers/
    start_help.py      # Команды /start и /help
    greetings.py       # Приветствие новых участников
    karma.py           # Обработка кармы
    moderation.py      # Система предупреждений
  services/
    karma_service.py   # Бизнес-логика кармы
    warn_service.py    # Бизнес-логика предупреждений
    admin_service.py   # Проверка прав администратора
  db/
    base.py           # Базовая конфигурация SQLAlchemy
    models.py         # Модели данных
    session.py        # Управление сессиями
    migrations/       # Миграции Alembic
```

## 🔒 Безопасность

- Токен бота хранится в `.env` (не коммитьте в Git!)
- Проверка прав администратора перед выполнением модераторских команд
- Анти-накрутка кармы (кулдаун между начислениями)
- Защита от спама приветствий

## 🐛 Устранение неполадок

### Бот не отвечает

1. Проверьте, что токен в `.env` правильный
2. Убедитесь, что бот добавлен в чат и имеет права администратора
3. Проверьте логи на наличие ошибок

### Мут не работает

1. Убедитесь, что бот имеет право "Restrict members"
2. Проверьте логи бота — там будет сообщение о недостающих правах

### Карма не начисляется

1. Проверьте, что сообщение содержит одно из ключевых слов ("спасибо", "спс", "thx", etc.)
2. Убедитесь, что не нарушен кулдаун (60 минут по умолчанию)
3. Проверьте, что вы не пытаетесь начислить карму самому себе или боту

## 📄 Лицензия

MIT

## 🤝 Вклад

Приветствуются pull requests и issues!

