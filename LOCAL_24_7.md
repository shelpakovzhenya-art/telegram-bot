# 💻 Запуск бота локально 24/7 (БЕСПЛАТНО)

Если у вас есть компьютер, который может работать постоянно, можно запустить бота локально.

## 📋 Требования

1. ✅ Компьютер с Windows/Linux/Mac
2. ✅ Python 3.12+ установлен
3. ✅ Компьютер должен быть включен постоянно

## 🚀 Настройка автозапуска

### Windows: Автозапуск через Task Scheduler

1. Откройте **"Планировщик заданий"** (Task Scheduler)
2. Нажмите **"Создать задачу"**
3. Вкладка **"Общие"**:
   - Имя: `Telegram Bot`
   - Отметить: **"Выполнять для всех пользователей"**
   - Отметить: **"Выполнять с наивысшими правами"**
4. Вкладка **"Триггеры"**:
   - Нажмите **"Создать"**
   - Начало: **"При входе в систему"**
   - Отметить: **"Включено"**
   - Нажмите **"ОК"**
5. Вкладка **"Действия"**:
   - Нажмите **"Создать"**
   - Действие: **"Запустить программу"**
   - Программа: `python`
   - Аргументы: `-m app.main`
   - Рабочая папка: `C:\telegram-bot` (ваш путь)
   - Нажмите **"ОК"**
6. Вкладка **"Условия"**:
   - Снимите галочку **"Запускать только при питании от электросети"**
   - Отметьте **"Пробуждать компьютер для выполнения задачи"**
7. Вкладка **"Параметры"**:
   - Отметьте **"Выполнять задачу немедленно, если пропущен запланированный запуск"**
   - Отметьте **"При сбое запускать задачу повторно"**
   - Интервал: `1 минута`
   - Количество попыток: `3`
8. Нажмите **"ОК"**

### Windows: Автозапуск через ярлык в автозагрузке

1. Создайте файл `start_bot.bat` в папке проекта:
```batch
@echo off
cd /d "%~dp0"
python -m app.main
pause
```

2. Нажмите `Win+R`, введите `shell:startup`
3. Создайте ярлык для `start_bot.bat` в папке автозагрузки

### Linux: Автозапуск через systemd

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/home/ваш_пользователь/telegram-bot
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/python3 -m app.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## 🔋 Энергосбережение

Чтобы компьютер не уходил в спящий режим:

**Windows:**
1. Откройте **"Электропитание"**
2. Выберите **"Высокая производительность"**
3. Настройте: **"Никогда"** для спящего режима

**Linux:**
```bash
sudo systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target
```

## ✅ Готово!

Бот будет запускаться автоматически при включении компьютера и работать постоянно.

---

## 💡 Советы

- Используйте стабильное интернет-соединение
- Убедитесь, что компьютер не переходит в спящий режим
- Проверяйте логи периодически
- Настройте автоматические обновления системы


