version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-bot
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge

