# 🔄 Обновление кода на Replit

Инструкция по обновлению кода бота на Replit после изменений в GitHub.

## 🚀 Быстрое обновление (через GitHub)

### Вариант 1: Автоматическое обновление (если Repl подключен к GitHub)

1. **Убедитесь, что код обновлен в GitHub:**
   ```bash
   git add .
   git commit -m "Update: убран кулдаун кармы, улучшен keep_alive"
   git push origin main
   ```

2. **В Replit:**
   - Откройте ваш Repl
   - В левой панели найдите вкладку **"Version Control"** (иконка с ветками)
   - Нажмите **"Pull from GitHub"** или **"Sync"**
   - Replit автоматически загрузит последние изменения

3. **Перезапустите бота:**
   - Остановите бота (кнопка "Stop" или Ctrl+C)
   - Нажмите **"Run"** для запуска с новым кодом

### Вариант 2: Ручное обновление через консоль

1. **В Replit откройте консоль (Shell)**

2. **Остановите бота** (если запущен):
   - Нажмите кнопку "Stop" или Ctrl+C в консоли

3. **Обновите код из GitHub:**
   ```bash
   git pull origin main
   ```
   
   Если возникнет конфликт, используйте:
   ```bash
   git fetch origin
   git reset --hard origin/main
   ```
   ⚠️ **Внимание:** Это перезапишет локальные изменения!

4. **Установите зависимости (если нужно):**
   ```bash
   pip install -r requirements.txt
   ```

5. **Запустите бота:**
   - Нажмите кнопку **"Run"** в Replit
   - Или в консоли: `python -m app.main`

## 📝 Пошаговая инструкция (детальная)

### Шаг 1: Подготовка

1. Убедитесь, что все изменения закоммичены и запушены в GitHub:
   ```bash
   git status
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

### Шаг 2: Обновление на Replit

**Способ А: Через интерфейс Replit (рекомендуется)**

1. Откройте ваш Repl на [replit.com](https://replit.com)
2. В левой панели найдите вкладку **"Version Control"** (иконка с ветками Git)
3. Если видите кнопку **"Pull"** или **"Sync"** - нажмите её
4. Если нет такой кнопки, перейдите к Способу Б

**Способ Б: Через консоль Replit**

1. Откройте консоль (Shell) в Replit
2. Выполните команды:
   ```bash
   # Проверьте текущий статус
   git status
   
   # Получите последние изменения
   git fetch origin
   
   # Обновите код
   git pull origin main
   ```

**Если возникли конфликты:**

```bash
# Сохраните текущие изменения (если нужно)
git stash

# Принудительно обновите до версии из GitHub
git fetch origin
git reset --hard origin/main

# Восстановите сохраненные изменения (если нужно)
git stash pop
```

### Шаг 3: Установка зависимостей

Если в `requirements.txt` появились новые зависимости:

```bash
pip install -r requirements.txt
```

### Шаг 4: Перезапуск бота

1. **Остановите текущий бот:**
   - Нажмите кнопку **"Stop"** в Replit
   - Или нажмите Ctrl+C в консоли

2. **Запустите бот заново:**
   - Нажмите кнопку **"Run"** (зеленая кнопка вверху)
   - Или в консоли: `python -m app.main`

3. **Проверьте логи:**
   - Убедитесь, что бот запустился без ошибок
   - Должно быть сообщение: `✅ Keep-alive server started successfully`
   - Должно быть сообщение: `Starting bot...`

### Шаг 5: Проверка работы

1. **Проверьте веб-сервер:**
   - Откройте URL вашего Repl в браузере
   - Должно быть сообщение: `Bot is running! Uptime: Xs`

2. **Проверьте бота в Telegram:**
   - Отправьте команду `/start` боту
   - Проверьте работу кармы (должна начисляться без кулдауна)

3. **Проверьте UptimeRobot:**
   - Откройте Dashboard UptimeRobot
   - Убедитесь, что статус монитора **"Up"** (зеленый)

## 🔧 Решение проблем

### Проблема: "git: command not found"

**Решение:**
Replit должен иметь Git по умолчанию. Если его нет:
1. В консоли выполните: `replit:upkg install git`
2. Или используйте интерфейс Replit для обновления

### Проблема: "Permission denied" при git pull

**Решение:**
```bash
# Проверьте права доступа
git config --global user.name "ваш-username"
git config --global user.email "ваш-email"

# Попробуйте снова
git pull origin main
```

### Проблема: Конфликты при слиянии

**Решение:**
Если есть конфликты, и вы хотите использовать версию из GitHub:
```bash
git fetch origin
git reset --hard origin/main
```

⚠️ **Внимание:** Это удалит все локальные изменения в Replit!

### Проблема: Бот не запускается после обновления

**Решения:**
1. Проверьте логи в консоли Replit на наличие ошибок
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте переменные окружения в Secrets (BOT_TOKEN и т.д.)
4. Попробуйте перезапустить Repl полностью

### Проблема: Keep-alive не работает

**Решения:**
1. Убедитесь, что файл `keep_alive.py` присутствует в корне проекта
2. Проверьте, что Flask установлен: `pip install flask`
3. Проверьте логи - должно быть сообщение о запуске keep-alive сервера

## ✅ Чек-лист обновления

- [ ] Код обновлен в GitHub
- [ ] Код обновлен в Replit (git pull)
- [ ] Зависимости установлены (pip install -r requirements.txt)
- [ ] Бот остановлен
- [ ] Бот запущен заново
- [ ] Keep-alive сервер работает (есть сообщение в логах)
- [ ] Веб-сервер отвечает (проверка URL в браузере)
- [ ] Бот отвечает в Telegram
- [ ] Карма работает без кулдауна
- [ ] UptimeRobot показывает статус "Up"

## 🎯 Быстрая команда для обновления

Если вы уверены, что хотите обновить код до версии из GitHub:

```bash
# Остановите бота (Ctrl+C или кнопка Stop)
# Затем в консоли:
git fetch origin && git reset --hard origin/main && pip install -r requirements.txt
# Затем запустите бота (кнопка Run)
```

⚠️ **Внимание:** Это удалит все локальные изменения!

---

## 📌 Рекомендации

1. **Всегда коммитьте изменения в GitHub** перед обновлением на Replit
2. **Делайте бэкапы** важных данных (база данных и т.д.)
3. **Тестируйте изменения локально** перед деплоем на Replit
4. **Проверяйте логи** после каждого обновления
5. **Используйте UptimeRobot** для мониторинга работы бота

---

## 🔄 Автоматическое обновление (продвинутый вариант)

Если вы хотите настроить автоматическое обновление при пуше в GitHub, можно использовать GitHub Actions или webhook, но это требует дополнительной настройки и выходит за рамки базовой инструкции.

